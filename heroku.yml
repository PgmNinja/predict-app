build:
  docker:
    web: Dockerfile.prod
    celery_worker: Dockerfile.prod
    celery_beat: Dockerfile.prod
run:
  web: gunicorn core.wsgi --bind 0.0.0.0:8000
  celery_worker: celery -A core worker -l info
  celery_beat: celery -A core worker -l info -B