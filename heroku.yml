build:
  docker:
    backend: prediction-app-backend/Dockerfile.backend
    nginx: prediction-app-frontend/predictionAPI/Dockerfile.frontend
  
run:
  web: gunicorn prediction-app-backend/core.wsgi --bind 0.0.0.0:8000
